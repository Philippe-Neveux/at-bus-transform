name: Update Airflow Submodule

on:
  push:
    branches: [ main ]

jobs:
  update-submodule:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Setup SSH
      uses: webfactory/ssh-agent@v0.8.0
      with:
        ssh-private-key: ${{ secrets.GCP_VM_SSH_PRIVATE_KEY }}
        
    - name: Add GCP VM to known hosts
      run: |
        ssh-keyscan -H ${{ secrets.GCP_VM_IP }} >> ~/.ssh/known_hosts
        
    - name: Update submodule on GCP VM
      run: |
        ssh ${{ secrets.GCP_VM_USER }}@${{ secrets.GCP_VM_IP }} << 'EOF'
          cd airflow-deploy/dags/db-at-bus-transform
          git pull origin main
          echo "Submodule updated successfully"
        EOF
        
    # - name: Restart Airflow services (optional)
    #   run: |
    #     ssh ${{ secrets.GCP_VM_USER }}@${{ secrets.GCP_VM_IP }} << 'EOF'
    #       # Restart Airflow webserver and scheduler if needed
    #       sudo systemctl restart airflow-webserver
    #       sudo systemctl restart airflow-scheduler
    #       echo "Airflow services restarted"
    #     EOF
    #   if: ${{ github.event_name == 'push' }} 